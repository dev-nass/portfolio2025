ScrollReveal().reveal("#hero", {
    origin: "top",
    distance: "50px",
    duration: 1000,
    delay: 200,
    reset: false, // Optional: allows the animation to play again on each scroll
});

ScrollReveal().reveal("#aboutme", {
    origin: "top",
    distance: "50px",
    duration: 1000,
    delay: 200,
    reset: false, // Optional: allows the animation to play again on each scroll
});

ScrollReveal().reveal("#vlv", {
    origin: "left",
    distance: "50px",
    duration: 1500,
    delay: 200,
    reset: false, // Optional: allows the animation to play again on each scroll
});

ScrollReveal().reveal("#norte_cafe", {
    origin: "right",
    distance: "50px",
    duration: 1500,
    delay: 200,
    reset: false, // Optional: allows the animation to play again on each scroll
});

ScrollReveal().reveal("#php_template", {
    origin: "left",
    distance: "50px",
    duration: 1500,
    delay: 200,
    reset: false, // Optional: allows the animation to play again on each scroll
});

ScrollReveal().reveal("#tweak_me", {
    origin: "right",
    distance: "50px",
    duration: 1500,
    delay: 200,
    reset: false, // Optional: allows the animation to play again on each scroll
});